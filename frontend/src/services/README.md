# Services

Frontend API clients and service adapters.

## Structure
- `api/` — HTTP client setup and shared request helpers
- `auth/` — authentication service calls
- `detection/` — detection submission and result polling
