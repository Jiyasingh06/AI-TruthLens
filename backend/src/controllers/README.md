# Controllers

HTTP request handlers mapped from routes.

## Structure
- `auth/` — register, login, logout, token refresh
- `detection/` — accept multimodal inputs and start analysis
- `history/` — detection history queries
- `reports/` — report retrieval and export
- `users/` — user profile operations
