# Services

Business logic and integrations.

## Structure
- `auth/` — credential and token logic
- `detection/` — detection job orchestration
- `history/` — history persistence helpers
- `reports/` — report generation helpers
- `ai-client/` — HTTP client to FastAPI AI backend
- `storage/` — file storage abstraction
