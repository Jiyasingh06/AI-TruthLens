# Backend

Express.js API gateway for AI TruthLens.

Responsibilities:
- Authentication and authorization
- User, history, and report persistence
- File upload intake
- Orchestration calls to the AI backend
- Serving detection metadata to the frontend

Source lives under `src/`. Runtime uploads and generated reports live under `uploads/`, `reports/`, and `history/`.
