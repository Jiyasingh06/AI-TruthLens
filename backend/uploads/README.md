# Uploads

Temporary and persisted user media uploaded for detection.

## Structure
- `images/` `videos/` `audio/` `documents/` `temp/`

Do not commit real user media. Keep this directory git-tracked via placeholders only.
