# AI Models

Wrappers, loaders, and inference adapters for modality-specific ML models.

## Structure
- `text/` — NLP / fake-news models
- `image/` — image deepfake / forgery models
- `video/` — video deepfake models
- `audio/` — audio deepfake / spoof models
- `multimodal/` — fusion / late-ensemble models
